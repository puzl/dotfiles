# do funcs first
RCS=(bash_funcs bash_aliases)

for rc in $RCS
do
    if [[ -f $HOME/.bash.d/$rc ]]; then
        source $HOME/.bash.d/$rc
    fi
done

for file in ~/.zsh.d/*.zsh
do
    source $file
done
